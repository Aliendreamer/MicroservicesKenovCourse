-- list of dependencies I may need and not to forget secrets will be in the appsettings which is in git ignore
.net api on 5000
mssql on 5001
--ms server 2019 docker container cant run on my arch for some reason so we go 2017
 docker run -e "ACCEPT_EULA=Y" -e "SA_PASSWORD=kenovCourse10" \
   -p 5001:1433 --name courseDb \
   -d mcr.microsoft.com/mssql/server:2017-latest

/seq container start command should make docker compose at one point
docker run \
  -d \
  --restart unless-stopped \
  --name seq \
  -e ACCEPT_EULA=Y \
  -v $HOST_PATH_TO_SEQ:/data \
  -p 8081:80 \
  datalust/seq:latest
